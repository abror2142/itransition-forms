api_refresh_token:
    path: /api/token/refresh

api_token_invalidate:
    path: /api/token/invalidate

api_login_check:
    path: /auth

controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
